// Allow CSS side-effect imports (e.g. `import './globals.css'` in layout.tsx).
// TypeScript 5.x with moduleResolution:"bundler" requires an explicit
// declaration for non-TS file imports.
declare module "*.css" {}
